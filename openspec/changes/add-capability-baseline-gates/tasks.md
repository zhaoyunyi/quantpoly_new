## 1. 建立能力基线

- [x] 1.1 输出能力等价矩阵（用户旅程 × 限界上下文）
- [x] 1.2 为每条能力定义 Given/When/Then 场景
- [x] 1.3 定义“功能缺失”的判定规则（阻断切换）

## 2. 建立波次门禁

- [x] 2.1 定义 Wave 切换前检查清单（鉴权、核心写路径、核心读路径、权限拒绝路径、异常路径）
- [x] 2.2 定义 Wave 切换后观察指标与阈值（错误率、延迟、任务积压、跨用户数据泄露）
- [x] 2.3 定义失败回滚触发与执行规则

## 3. 挂接流程与验证

- [x] 3.1 将能力门禁加入迁移提案评审要求
- [x] 3.2 为门禁清单补充 CLI 校验入口（JSON 输出）
- [x] 3.3 运行 `openspec validate add-capability-baseline-gates --strict`
