# update-realtime-monitoring-auth Tasks

## 1. 规范

- [ ] 1.1 创建 `monitoring-realtime` delta spec（`openspec/changes/update-realtime-monitoring-auth/specs/monitoring-realtime/spec.md`）

## 2. 实现（后续执行阶段）

- [ ] 2.1 更新 WS 鉴权：使用 `user-auth` 的 session token
- [ ] 2.2 增加未认证连接的测试用例（返回 4401）

